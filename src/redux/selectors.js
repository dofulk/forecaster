export const locationSelecter = state => state.location

export const possibleLocationsSelector = state => state.possibleLocations

export const currentWeatherSelector = state => state.currentWeather

export const forecastSelector = state => state.forecast